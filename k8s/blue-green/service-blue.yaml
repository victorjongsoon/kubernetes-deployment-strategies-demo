apiVersion: v1
kind: Service
metadata:
  name: k8s-deploy-demo
  namespace: k8s-demo
  labels:
    app: k8s-deploy-demo
spec:
  type: LoadBalancer
  selector:
    app: k8s-deploy-demo
    color: blue
  ports:
    - name: http
      port: 80
      targetPort: 5000