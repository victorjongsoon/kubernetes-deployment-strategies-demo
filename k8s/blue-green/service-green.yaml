apiVersion: v1
kind: Service
metadata:
  name: k8s-deploy-demo
  namespace: k8s-demo
  labels:
    app: k8s-deploy-demo
spec:
  type: ClusterIP
  selector:
    app: k8s-deploy-demo
    color: green
  ports:
    - name: http
      port: 80
      targetPort: 5000
